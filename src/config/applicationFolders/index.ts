import env from '../../environment/environment';

export const ROOT_FOLDER = env.ROOT_FOLDER;
export const SONG_STORAGE = env.SONG_STORAGE;
export const GAME_IMAGES_STORAGE = env.GAME_IMAGES_STORAGE;
